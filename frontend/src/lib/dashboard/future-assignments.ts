/**
 * Searchable assignments for extra completed work (modal).
 * Demo catalog lives in mock; re-export keeps a stable import path for API swap later.
 */

export { mockFutureAssignments as FUTURE_ASSIGNMENTS } from "@/lib/mock/demo-dashboard-data";
